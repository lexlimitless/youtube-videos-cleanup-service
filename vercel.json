{
  "routes": [
    { "src": "/api/.*", "dest": "/api" },
    { "src": "/redirect/:id", "dest": "/api/redirect/:id" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "buildCommand": "yarn build",
  "installCommand": "yarn install",
  "outputDirectory": "dist",
  "framework": "vite"
} 